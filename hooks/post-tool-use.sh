#!/usr/bin/env bash
# PostToolUse hook — live-signature-update (Day 5 target)
#
# When the user edits a file via Write/Edit, trigger a lightweight
# signature refresh so the signature JSON visibly updates during a session.
#
# Day 1: stub only. Full implementation lands Day 5 after core flow is working.

set -euo pipefail

# Placeholder — reads stdin JSON from Claude Code hook, no-op for now
cat >/dev/null

exit 0
